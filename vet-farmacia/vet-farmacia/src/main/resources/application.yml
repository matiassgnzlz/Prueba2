spring:
  application:
    name: vet-farmacia
  datasource:
    url: jdbc:mysql://localhost:3306/db_farmacia
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/farmacia.sql

server:
  port: 8085

jwt:
  secret: clave_super_secreta_12345678901234567890

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true