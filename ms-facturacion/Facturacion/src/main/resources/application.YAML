spring:
  application:
    name: facturacion

  datasource:
    url: jdbc:mysql://localhost:3306/ms_facturacion
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

  instance:
    prefer-ip-address: true

jwt:
  secret: miclavesupersecretaparajwt2026segura123

springdoc:
  swagger-ui:
    path: /swagger-ui.html